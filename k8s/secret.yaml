apiVersion: v1
kind: Secret
metadata:
  name: adsb-secrets
  namespace: adsb-tracker
type: Opaque
stringData:
  # Database password (matches docker-compose for consistency)
  DB_PASSWORD: "adsb"
  POSTGRES_PASSWORD: "adsb"
